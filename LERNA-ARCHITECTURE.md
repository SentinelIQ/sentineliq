# 🔧 Arquitetura Lerna + Wasp - Visualização

## 📊 Fluxo de Dados

```
┌─────────────────────────────────────────────────────────────┐
│                   sentineliq MONOREPO                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  packages/ (Gerenciado por Lerna)                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                                                      │  │
│  │  ┌──────────────────┐  ┌──────────────────┐        │  │
│  │  │ shared-types     │  │ validators       │        │  │
│  │  ├──────────────────┤  ├──────────────────┤        │  │
│  │  │ • User types     │  │ • Zod schemas    │        │  │
│  │  │ • Workspace      │  │ • Form rules     │        │  │
│  │  │ • Domain models  │  │ • API validation │        │  │
│  │  └────────┬─────────┘  └────────┬─────────┘        │  │
│  │           │                     │                  │  │
│  │  ┌────────▼─────────┐  ┌────────▼─────────┐        │  │
│  │  │ ui-components    │  │ utils            │        │  │
│  │  ├──────────────────┤  ├──────────────────┤        │  │
│  │  │ • Button         │  │ • formatDate     │        │  │
│  │  │ • Modal          │  │ • formatCurrency │        │  │
│  │  │ • Input          │  │ • crypto utils   │        │  │
│  │  └──────────────────┘  └──────────────────┘        │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                           ▲                                │
│                           │                                │
│                  npm workspaces (symlinks)                │
│                           │                                │
│           ┌───────────────┼───────────────┐                │
│           │               │               │                │
│  ┌────────▼─────┐  ┌──────▼──────┐  ┌────▼──────┐         │
│  │     app/     │  │   apps/     │  │ e2e-tests │         │
│  │ (Wasp)       │  │   blog      │  │           │         │
│  ├──────────────┤  ├─────────────┤  ├───────────┤         │
│  │ • UI         │  │ • Astro     │  │ Playwright│         │
│  │ • Server     │  │ • Content   │  │           │         │
│  │ • Database   │  │ • Blog      │  └───────────┘         │
│  └──────────────┘  └─────────────┘                        │
│                                                             │
│ ⚠️  SentinelIQ (app/) NÃO é gerenciado por Lerna         │
│ ✅  Só packages/ são publicados no npm                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📂 Estrutura de Diretórios

```
sentineliq/
│
├── 📄 package.json                 ← Root (workspaces + Lerna script)
├── 📄 lerna.json                   ← Configuração Lerna
├── 📄 tsconfig.json                ← TypeScript base
├── 📄 LERNA-*.md                   ← Documentação
│
├── 📦 packages/                    ← ✅ Gerenciado por Lerna
│   │
│   ├── shared-types/
│   │   ├── package.json            (@sentineliq/shared-types)
│   │   ├── tsconfig.json
│   │   └── src/
│   │       ├── user.ts
│   │       ├── workspace.ts
│   │       └── index.ts
│   │
│   ├── validators/
│   │   ├── package.json            (@sentineliq/validators)
│   │   ├── tsconfig.json
│   │   └── src/
│   │       ├── user.ts
│   │       ├── form.ts
│   │       └── index.ts
│   │
│   ├── utils/
│   │   ├── package.json            (@sentineliq/utils)
│   │   ├── tsconfig.json
│   │   └── src/
│   │       ├── formatting.ts
│   │       ├── crypto.ts
│   │       └── index.ts
│   │
│   └── ui-components/
│       ├── package.json            (@sentineliq/ui-components)
│       ├── tsconfig.json
│       └── src/
│           ├── Button.tsx
│           ├── Modal.tsx
│           └── index.ts
│
├── 📱 apps/                        ← npm workspaces
│   ├── blog/
│   │   └── package.json            (Astro)
│   │
│   └── e2e-tests/
│       └── package.json            (Playwright)
│
├── 🟣 app/ (sentineliq)            ← ⚠️ Wasp (NÃO em Lerna)
│   ├── main.wasp
│   ├── package.json
│   ├── src/
│   │   ├── client/
│   │   ├── server/
│   │   └── core/
│   └── .wasp/
│
├── 🔧 .github/
│   └── workflows/
│       ├── test.yml               ← CI tests
│       └── publish.yml            ← CD publish (Lerna)
│
└── 📚 docs/
    └── ...                        ← Documentação existente
```

---

## 🔄 Ciclo de Desenvolvimento

```
┌──────────────────────────────────────────────────────────────┐
│           CICLO DE DESENVOLVIMENTO                           │
└──────────────────────────────────────────────────────────────┘

1️⃣  DESENVOLVIMENTO
   ┌────────────────────────────────────────────────────────┐
   │ Terminal 1: Wasp                                       │
   │ $ npm run dev                                          │
   │                                                        │
   │ Terminal 2: Modificar packages                         │
   │ $ cd packages/shared-types                             │
   │ $ echo "export type NewType = {}" >> src/types.ts    │
   │                                                        │
   │ ✅ Wasp detecta mudança e recompila automaticamente  │
   └────────────────────────────────────────────────────────┘

2️⃣  VERSIONAR
   ┌────────────────────────────────────────────────────────┐
   │ $ npm run lerna:version                                │
   │                                                        │
   │ Lerna analisa commits convencionais e sugere:         │
   │  @sentineliq/shared-types: 1.0.0 → 1.1.0 (feature)   │
   │  @sentineliq/validators:   1.0.0 → 1.0.0 (unchanged) │
   │  ...                                                    │
   │                                                        │
   │ Cria tags e commit de release                         │
   └────────────────────────────────────────────────────────┘

3️⃣  PUBLICAR
   ┌────────────────────────────────────────────────────────┐
   │ $ npm run lerna:publish                                │
   │                                                        │
   │ Publica no npm:                                        │
   │ ✅ @sentineliq/shared-types@1.1.0                    │
   │ ⏭️  @sentineliq/validators@1.0.0 (unchanged)         │
   │ ⏭️  @sentineliq/utils@1.0.0 (unchanged)              │
   │ ⏭️  @sentineliq/ui-components@1.0.0 (unchanged)      │
   │                                                        │
   │ ⚠️  app/ NÃO é publicado                              │
   └────────────────────────────────────────────────────────┘
```

---

## 🔗 Resolução de Importações

```
Quando você importa em app/src:

import type { User } from '@sentineliq/shared-types';
                      │
                      └─────┐
                            ▼
npm resolve em node_modules:
node_modules/@sentineliq/
└── shared-types           ← symlink para packages/shared-types/
    └── package.json       (main: "src/index.ts")
        └── src/
            ├── user.ts
            └── index.ts   ← Encontra exports!

✅ Automático via npm workspaces
✅ Sem necessidade de build entre packages
```

---

## 📊 Comparação: Antes vs Depois

### ❌ ANTES (Sem Lerna)

```
app/
├── src/
│   ├── client/
│   │   ├── components/
│   │   │   ├── Button.tsx
│   │   │   └── Modal.tsx
│   │   └── pages/
│   │
│   ├── server/
│   │   └── ...
│   │
│   └── shared/
│       ├── types.ts        ← Tipos espalhados
│       ├── validators.ts   ← Validators espalhados
│       └── utils.ts        ← Utils espalhados
│
├── node_modules/
│   ├── react
│   ├── zod
│   └── ...

Problemas:
❌ Difícil compartilhar código
❌ Tipos acoplados ao Wasp
❌ Componentes reutilizáveis dentro de app
❌ Impossível publicar packages
```

### ✅ DEPOIS (Com Lerna)

```
packages/
├── shared-types/
│   └── src/
│       └── types.ts        ← Tipos organizados
├── validators/
│   └── src/
│       └── validators.ts   ← Validators independente
├── utils/
│   └── src/
│       └── utils.ts        ← Utils independente
└── ui-components/
    └── src/
        ├── Button.tsx      ← Componentes reutilizáveis
        └── Modal.tsx

app/
└── src/
    └── Importa de @sentineliq/*

node_modules/
├── @sentineliq/
│   ├── shared-types       ← symlink
│   ├── validators         ← symlink
│   ├── utils              ← symlink
│   └── ui-components      ← symlink
└── ...

Benefícios:
✅ Código organizado e modular
✅ Tipos independentes
✅ Componentes reutilizáveis
✅ Packages publicáveis
✅ Versionamento sem quebra
✅ Escalável
```

---

## 🚀 Fluxo CI/CD

```
┌─────────────────────────────────────────────────────────────┐
│             CI/CD Pipeline com Lerna                        │
└─────────────────────────────────────────────────────────────┘

1. Push para main
   │
   ├─ Test Job
   │  ├─ npm install --workspaces
   │  ├─ npm run type-check
   │  ├─ npm run build:packages
   │  ├─ npm run test
   │  ├─ npm run lint
   │  └─ npm run build -w app
   │
   └─ Publish Job (se test passou)
      ├─ npm install --workspaces
      ├─ npm run build:packages
      ├─ lerna publish --yes          ← Publica no npm
      └─ GitHub cria release

✅ SentinelIQ (app/) não afeta publicações
✅ Apenas packages/ são publicados
✅ Versioning automático
```

---

## 🎯 Quando usar cada Componente

```
┌────────────────────────────────────────────────────────────┐
│ packages/shared-types                                      │
├────────────────────────────────────────────────────────────┤
│ Use para: Types TypeScript puros, interfaces, domains     │
│ Exemplos:                                                   │
│  - User, Workspace, Alert types                           │
│  - API response types                                      │
│  - Database schemas (interface level)                     │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ packages/validators                                        │
├────────────────────────────────────────────────────────────┤
│ Use para: Zod schemas, validações reutilizáveis          │
│ Exemplos:                                                   │
│  - userSignupSchema                                       │
│  - formValidationSchema                                   │
│  - APIRequestSchema                                       │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ packages/utils                                             │
├────────────────────────────────────────────────────────────┤
│ Use para: Funções utilitárias reutilizáveis              │
│ Exemplos:                                                   │
│  - formatDate, formatCurrency                             │
│  - cryptographic functions                                │
│  - data transformations                                   │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ packages/ui-components                                     │
├────────────────────────────────────────────────────────────┤
│ Use para: Componentes React reutilizáveis                │
│ Exemplos:                                                   │
│  - Button, Modal, Input                                   │
│  - Layout components                                      │
│  - UI primitives                                          │
└────────────────────────────────────────────────────────────┘

├────────────────────────────────────────────────────────────┤
│ app/ (SentinelIQ) - NÃO em Lerna                          │
├────────────────────────────────────────────────────────────┤
│ Use para: Lógica da plataforma SentinelIQ específica      │
│ Exemplos:                                                   │
│  - Page components                                        │
│  - Server actions/queries                                 │
│  - Business logic                                         │
│  - Database operations                                    │
└────────────────────────────────────────────────────────────┘
```

---

## 💡 Tips & Tricks

```bash
# 🔍 Ver qual arquivo precisa build
npm ls @sentineliq/shared-types

# 📊 Visualizar todas as dependências
npx lerna list --graph --all

# 🔄 Recompilar um package específico
npm run build -w @sentineliq/shared-types

# 🧹 Limpeza completa
npm run clean --workspaces
rm -rf node_modules
npm install --workspaces

# 📋 Ver histórico de versionamento
git log --oneline | grep "chore(release)"

# 🚀 Publicar apenas um package
lerna publish --scope @sentineliq/shared-types
```

---

## 🎓 Resumo Visual

```
sentineliq/
├─ packages/        → Lerna ✅
│  ├─ shared-types
│  ├─ validators
│  ├─ utils
│  └─ ui-components
│
├─ apps/            → npm workspaces ✅
│  ├─ blog
│  └─ e2e-tests
│
├─ app/             → Wasp (não toca) ⚠️
│
└─ Resultado: Monorepo escalável! 🚀
```
