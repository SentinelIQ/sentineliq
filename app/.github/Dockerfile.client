FROM nginx:alpine

# Copy built web app files to nginx
COPY .wasp/build/web-app /usr/share/nginx/html/

# Copy nginx config
COPY .github/nginx.conf /etc/nginx/nginx.conf

EXPOSE 3000

CMD ["nginx", "-g", "daemon off;"]
